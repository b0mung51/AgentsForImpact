# My New Repo

## Webcam Test (OpenCV, macOS)

1. Install OpenCV:

```bash
pip install opencv-python
```

2. Run the webcam test:

```bash
python camera_test.py
```

Controls:
- Press `q` to quit.
- Press `Esc` to quit.
